const SectionBadge = ({ children }: { children: React.ReactNode }) => (
  <div className="liquid-glass rounded-full px-3.5 py-1 text-xs font-medium text-foreground font-body inline-block">
    {children}
  </div>
);

export default SectionBadge;
